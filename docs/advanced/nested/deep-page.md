# Deep Page

A page three levels deep verifies that relative asset paths, sidebar links and breadcrumbs work at any nesting depth.

---

## Relative Paths

This page lives at `/advanced/nested/deep-page`, so all assets must be
loaded with a `../../` prefix. If the styles look correct, the path
calculation works.

## Sidebar

The left sidebar lists only the pages from this nested category.

## One More Heading

Just to make the table of contents a bit longer.
